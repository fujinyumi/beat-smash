using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GradeColor : MonoBehaviour {

    private enum Grade { A, B, C, D, F, Error };

    //private Grade myGrade;

	// Use this for initialization
	void Start () {
        //not completed at this time
        /*
        Text myText = GetComponent<Text>();
        
        if(myText.text == "A")
        {
            myGrade = Grade.A;
        }
        else if (myText.text == "B")
        {
            myGrade = Grade.A;
        }
        else if (myText.text == "C")
        {
            myGrade = Grade.A;
        }
        else if (myText.text == "D")
        {
            myGrade = Grade.A;
        }
        else if (myText.text == "F")
        {
            myGrade = Grade.A;
        }
        else
        {
            Debug.Log("Somehow, a grade couldn't be aqcuired.");
            myGrade = Grade.Error;
        }
        */
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
